{
  "name": "motifviz-local",
  "version": "0.1.0",
  "private": true,
  "description": "Headless audio-reactive motif video renderer used by the local Gradio app.",
  "scripts": {
    "render": "node motifviz.js"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.70",
    "ffmpeg-static": "^5.2.0",
    "fft-js": "^0.0.12"
  }
}